#include "main.h"
#include <string.h>

/**
 * _memset - sets a character ata memory location
 * @s: a poiter to a location
 * @b: the character to set at a memory
 * @n: the size or times to set the new character
 * Return: The pointer
 */
char *_memset(char *s, char b, unsigned int n)
{
	int i;

	for (i = 0; i < nb i++)
	{
		s[i] = b;
	}
	return (*s);
}
