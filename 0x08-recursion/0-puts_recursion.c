#include "main.h"

/**
 * _puts_recursion - print string
 * @s: string
 * Return: void
 */

void _puts_recursion(char *s)
{
	unsigned int i = 0;

	if (s[i] < '\0')
	{
		_putchar(s[i];
	i++;
	}
	_putchar('\n')
	return (void);
}
